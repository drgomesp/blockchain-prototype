syntax = "proto3";

package service;
option go_package = "github.com/drgomesp/rhizom/proto/gen/service";

import "proto/message.proto";

service Node {
  rpc GetBlock (stream message.GetBlockRequest) returns (stream message.GetBlockResponse);
}
